import 'package:flutter/material.dart';

import 'login_page.dart';

class LoginPageState extends State<LoginPage> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Authentication page')));
  }
}
